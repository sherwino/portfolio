import { StackFilterPipe } from './stack-filter.pipe';

describe('StackFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StackFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
